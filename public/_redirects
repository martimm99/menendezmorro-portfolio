# Netlify redirects

# SPA shell: any path that doesn't match an existing file falls through to
# the home shell. main.js inspects the pathname and renders the matching
# page (Home, Project placeholder, Contact placeholder). Phase 6 and Phase 9
# will replace this with explicit /contact /contact.html and /* /project.html
# rewrites once those templates exist.
/*    /index.html   200

# Phase 12 will add 301s for legacy hash URLs listed in BUILD_SPEC.md §4
# (/#morro -> /morro, /#gestion_reaviva -> /reaviva, etc.).
